Підрахунок частоти символів у рядку:
Напиши код, який приймає рядок тексту і створює словник, де ключами є символи, а значеннями — кількість їх появ у тексті.

Злиття двох словників з обробкою конфліктів:
Напиши код, який об'єднує два словники. Якщо ключі збігаються, значення повинні бути об'єднані у список.

Інвертування словника з унікальними значеннями:
Напиши код, який приймає словник з унікальними значеннями і створює новий словник, де ключами є значення початкового словника, а значеннями — ключі початкового словника.

Сортування словника за ключами:
Напиши код, який сортує словник за його ключами і створює новий словник, відсортований за зростанням або спаданням ключів.

Пошук ключів за частковим збігом значень:
Напиши код, який приймає словник і рядок, і повертає список ключів, значення яких містять даний рядок.

Перетворення списку кортежів у словник:
Напиши код, який приймає список кортежів (де кожен кортеж складається з двох елементів) і перетворює його на словник. Наприклад, [(1, 'a'), (2, 'b'), (3, 'c')] -> {1: 'a', 2: 'b', 3: 'c'}.

Групування елементів списку за значеннями:
Напиши код, який приймає список кортежів (де кожен кортеж складається з двох елементів) і створює словник, де ключами є перші елементи кортежів, а значеннями — списки других елементів кортежів, згрупованих за першим елементом. Наприклад, [(1, 'a'), (2, 'b'), (1, 'c')] -> {1: ['a', 'c'], 2: ['b']}.

Об'єднання списків значень зі спільними ключами:
Напиши код, який приймає два словники, де значеннями є списки, і створює новий словник, де значеннями є об'єднані списки зі спільними ключами. Наприклад, {'a': [1, 2], 'b': [3, 4]} і {'a': [5], 'b': [6, 7]} -> {'a': [1, 2, 5], 'b': [3, 4, 6, 7]}.

Пошук максимального та мінімального значення у словнику:
Напиши код, який приймає словник з числовими значеннями і знаходить ключі з максимальним та мінімальним значеннями.

Підрахунок сум значень у вкладених словниках:
Напиши код, який приймає словник, значеннями якого є інші словники з числовими значеннями, і підраховує суму всіх числових значень. Наприклад, {'a': {'x': 1, 'y': 2}, 'b': {'x': 3, 'y': 4}} -> 10.